{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Modules
    "module": "preserve",
    "moduleResolution": "bundler",

    // Language and Environment
    "jsx": "preserve",
    "lib": ["DOM", "ESNext"],
  },
}
